name: "ESBuild Testing: EXT-Background"

on: [pull_request]

jobs:
  eslint:
    name: Run esbuild
    runs-on: ubuntu-latest
    steps:
      - name: "Use Node.js v20.x (latest)"
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          check-latest: true

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: npm prune

      - name: Install EXT-Background
        run: cd EXTs/EXT-Background && npm install

      - name: Run ESBuild
        run: cd EXTs/EXT-Background && npm run test:minify
        continue-on-error: false
