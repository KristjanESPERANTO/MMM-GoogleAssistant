const config = {
  tabWidth: 2,
  bracketSpacing: true,
  overrides: [
    {
      files: "*.md",
      options: {
        parser: "markdown"
      }
    }
  ],
  trailingComma: "none"
};

export default config;
